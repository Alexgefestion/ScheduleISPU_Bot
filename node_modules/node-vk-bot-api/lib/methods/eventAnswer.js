const toArray = require('../utils/toArray');

module.exports = function (eventId, userId, peerId) {
  // const [deleteForAll, spam, userId, messageId] = args;


  return this.execute(
    'messages.sendMessageEventAnswer',
    Object.assign(
      {
        event_id: eventId,
        user_id: userId,
        peer_id: peerId,
      },
    ),
  );
};